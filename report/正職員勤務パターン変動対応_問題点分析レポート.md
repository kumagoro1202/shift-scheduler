# 正職員勤務パターン変動対応 - 問題点分析レポート

**作成日**: 2026年1月4日  
**対象バージョン**: 0.0.4  
**分析対象**: 正職員の勤務パターン変動要求事項への対応状況

---

## 📋 エグゼクティブサマリー

### 新要求事項の概要

REQUIREMENTS.mdに正職員の勤務パターン変動要求事項が追加されました。この要求事項では以下が規定されています：

- **正職員（通常）**: 日によって早番・中番・遅番を変動的に割り当て
- **パート職員・時短勤務職員**: 登録された勤務形態で固定
- 最適化アルゴリズムが各正職員の負担とスキルバランスを考慮して自動割り当て

### 分析結果の総評

プロジェクト全体を総点検した結果、**重大な実装不足**が確認されました。

**主要な問題点**:
1. ✅ データモデル（`models.py`, `database.py`）は正しく実装済み
2. ❌ **最適化エンジン（`optimizer.py`）が未対応**（最も重大）
3. ✅ 休憩スケジューラ（`breaks.py`）は対応済み
4. ✅ UI層（職員管理画面）は対応済み
5. ⚠️ UI層（シフト生成・表示画面）は部分的対応
6. ⚠️ 設計書・ドキュメント類は記述あるが実装と乖離

---

## 🔍 詳細分析

### 1. データモデル層（✅ 対応済み）

#### models.py

**状況**: ✅ **完全対応**

```python
@dataclass(slots=True)
class Employee:
    # ... 省略 ...
    is_pattern_fixed: bool = False  # 勤務パターン固定フラグ
```

- `is_pattern_fixed`フィールドが正しく定義されている
- Falseのとき変動割り当て、Trueのとき固定

#### database.py

**状況**: ✅ **完全対応**

1. **テーブル定義**:
```sql
CREATE TABLE employees (
    -- ... 省略 ...
    is_pattern_fixed BOOLEAN DEFAULT 0,
    employment_pattern_id TEXT REFERENCES employment_patterns(id),
    -- ...
);
```

2. **マイグレーション処理**:
```python
def _migrate_employee_table():
    """Add is_pattern_fixed column if it doesn't exist."""
    # 既存のDBにも自動でカラム追加
```

3. **勤務パターンマスタ**:
- `full_early`, `full_mid`, `full_late` など3種類の正職員パターンが定義済み
- 月火金・水曜日・木土の異なるパターンは**未定義**（要追加）

**問題点**:
- ⚠️ **月火金用の3パターン（A/B/C）が未登録**
- ⚠️ **水曜日用の3パターン（◯/通常/△）が未登録**
- ⚠️ **木土用の3パターン（1/2/3）が未登録**
- 現在は汎用的な`full_early/mid/late`のみで、曜日別の対応が不完全

---

### 2. 最適化エンジン層（❌ **未対応** - 最重要課題）

#### optimizer.py

**状況**: ❌ **未実装**（最も重大な問題）

**現在の実装**:
```python
def _assign_employees_to_slot(available, slot, date_str, optimisation_mode, work_count, morning_workers):
    # 職員を選択するが、勤務パターンは考慮していない
    selected = _select_employees_for_slot(...)
    
    # 👇 ここで勤務パターンを動的に選択する必要があるが未実装
    for employee in selected:
        shift = GeneratedShift(
            # ... 省略 ...
            # employment_pattern_id が固定化されている
        )
```

**未実装の機能**:

1. **正職員の勤務パターン動的選択ロジック**
   ```python
   # 必要だが存在しない処理
   def _select_employment_pattern_for_regular_employee(employee, date, slot, existing_assignments):
       """正職員の勤務パターンを動的に選択する
       
       - 月火金: A/B/C から選択
       - 水曜日: ◯/通常/△ から選択
       - 木土: 1/2/3 から選択
       - 既存の割り当てを考慮して偏りを防止
       """
       pass  # ← 未実装
   ```

2. **パターンの偏り防止機構**
   - 同一職員が特定のパターン（早番など）に偏らないようにする
   - 勤務パターンカウンターの管理が必要

3. **曜日別パターンの判定**
   - 月火金、水曜日、木土で異なるパターンセットを使用
   - 現在はこの判定ロジックが存在しない

**影響範囲**:
- シフト生成時に正職員の勤務パターンが適切に割り当てられない
- 全員が同じパターン（例: `full_mid`）で固定化される可能性
- 要求仕様の核心部分が動作しない

**修正必要箇所**:
1. `generate_shifts`関数の拡張
2. `_assign_employees_to_slot`関数の改修
3. `GeneratedShift`生成時のパターンID決定ロジック追加
4. パターン使用頻度の追跡機構

---

### 3. 休憩スケジューラ層（✅ 対応済み）

#### breaks.py

**状況**: ✅ **対応済み**

```python
def _break_windows_for_pattern(pattern_break_hours: float):
    """勤務形態に応じた休憩ウィンドウを返す"""
    if pattern_break_hours >= 2:
        # 2時間休憩: 基本的に連続取得
        return [PREFERRED_WINDOWS[0], PREFERRED_WINDOWS[2]]
    if pattern_break_hours >= 1:
        return [PREFERRED_WINDOWS[1]]
    return []
```

- 勤務パターンの`break_hours`を参照して休憩時間を決定
- 正職員の2時間連続休憩に対応
- 動的な勤務パターンにも対応可能な設計

**問題点**: なし

---

### 4. UI層（⚠️ 部分的対応）

#### pages/1_👥_職員管理.py

**状況**: ✅ **対応済み**

1. **新規登録時の自動設定**:
```python
# is_pattern_fixed の自動設定
if employment_type == "正職員" and pattern_category == "full_time":
    is_pattern_fixed = False  # 変動
else:
    is_pattern_fixed = True  # 固定
```

2. **一覧表示**:
```python
# パターン固定アイコン
fixed_icon = "📌" if emp_data['is_pattern_fixed'] else "🔄"
fixed_text = "固定" if emp_data['is_pattern_fixed'] else "変動"
```

- 勤務パターンが「変動割り当て」か「固定」かを明示
- 正職員（通常）は「🔄変動」と表示される

**問題点**: なし

#### pages/3_🎯_シフト生成.py

**状況**: ⚠️ **部分的対応**

**対応済みの部分**:
```python
for shift in result_shifts:
    # 勤務パターンIDを取得（正職員は日ごとに変動、パート・時短は固定）
    pattern_id = shift.employee.employment_pattern_id if shift.employee else None
    shift_id = create_shift(shift.date, shift.time_slot_id, shift.employee_id, pattern_id)
```

- コメントで意図は記述されている
- DBへの保存時に`employment_pattern_id`を渡している

**問題点**:
- ❌ **optimizer.pyが対応していないため、実際には動作しない**
- 最適化モードの説明にスキル能力の平均化が記述されているが、勤務パターン変動については説明不足

#### pages/4_📋_シフト表示.py

**状況**: ⚠️ **部分的対応**

**対応済みの部分**:
```python
# 勤務パターンの凡例マッピング
pattern_legend = {
    "full_early": "早",
    "full_mid": "中",
    "full_late": "遅",
    # ...
}
```

**問題点**:
- ⚠️ **月火金の A/B/C、水曜の ◯/通常/△、木土の 1/2/3 に対応する凡例が未定義**
- 現在の凡例は汎用的な「早/中/遅」のみ
- シフト表示時に勤務パターンIDから適切な記号を表示する必要あり

---

### 5. 設計書・ドキュメント層（⚠️ 記述あるが実装と乖離）

#### design/シフト生成画面_詳細設計書.md

**状況**: ⚠️ **記述あるが実装されていない**

記述内容:
> - **勤務パターンの変動割り当て**: 正職員は日によって早番・中番・遅番を変動的に割り当て

実装状況:
- ❌ 最適化エンジンが未実装
- ドキュメントと実装が乖離している

#### design/職員管理画面_詳細設計書.md

**状況**: ✅ **記述と実装が一致**

正しく記述されている:
> | 職員分類 | employment_pattern_id | is_pattern_fixed | 説明 |
> |---------|---------------------|------------------|------|
> | 正職員（通常） | NULL または任意 | False | シフト生成時に早番・中番・遅番から選択 |

#### docs/REQUIREMENTS.md

**状況**: ✅ **詳細に記述されている**

要求仕様は明確:
```
#### 勤務パターンの割り当て方針

| 職員分類 | 勤務パターンの割り当て方法 |
|---------|---------------------------|
| 正職員 | **変動割り当て**: 日によって早番・中番・遅番を変えて割り当て |
| パート職員 | **固定割り当て**: 登録された勤務形態で固定 |
```

月火金・水曜日・木土の具体的なパターンも明記されている。

**問題点**:
- ドキュメントは完璧だが実装が追いついていない

#### docs/ALGORITHM.md

**状況**: ⚠️ **記述あるが実装と乖離**

記述内容:
> 正職員の勤務パターン選択、パート職員と時短勤務職員は登録された勤務形態で固定

実装状況:
- ❌ アルゴリズムドキュメントに記述があるが実装がない

#### docs/ARCHITECTURE.md

**状況**: ⚠️ **記述あるが実装不足**

勤務パターンマスタについて:
- 月火金・水曜日・木土の異なるパターンの存在が示されている
- `day_type`フィールドで曜日タイプを区別する設計
- しかし、database.pyの`_seed_employment_patterns`では汎用パターンのみ

#### docs/USER_GUIDE.md

**状況**: ⚠️ **説明あるが機能未実装**

ユーザーガイドには記述されているが:
> **パターン固定について**:
> - 🔄 **変動**: 正職員（通常）は日によって早番・中番・遅番を変動的に割り当て

実際には動作しない。

---

## 🚨 重大度別問題点リスト

### 🔴 Critical（重大・必須修正）

| No | 箇所 | 問題 | 影響 |
|----|------|------|------|
| 1 | `optimizer.py` | 正職員の勤務パターン動的選択ロジックが未実装 | **要求仕様の核心機能が動作しない** |
| 2 | `database.py` | 月火金・水曜・木土用の勤務パターンマスタが未登録 | パターン選択時に選択肢がない |
| 3 | `optimizer.py` | パターン偏り防止機構が未実装 | 同じパターンばかり選ばれる |

### 🟡 High（高・早期修正推奨）

| No | 箇所 | 問題 | 影響 |
|----|------|------|------|
| 4 | `pages/4_📋_シフト表示.py` | 月火金A/B/C、水曜◯/通常/△、木土1/2/3の凡例未定義 | 表示が分かりにくい |
| 5 | `pages/3_🎯_シフト生成.py` | 勤務パターン変動についてのユーザー説明不足 | ユーザーが機能を理解できない |
| 6 | `design/*.md` | 設計書と実装の乖離 | 保守性の低下 |

### 🟢 Medium（中・将来的に対応）

| No | 箇所 | 問題 | 影響 |
|----|------|------|------|
| 7 | `docs/*.md` | ドキュメントと実装の乖離 | 誤解を招く |
| 8 | `optimizer.py` | 統計情報にパターン分布が含まれていない | 運用時の検証が困難 |

---

## 📝 修正計画

### Phase 1: 最重要（Critical対応）

#### 1-1. 勤務パターンマスタの追加

**ファイル**: `database.py`

**作業内容**:

1. `_seed_employment_patterns`関数に以下を追加:

```python
# 月火金の3パターン
("weekday_full_A", "正職員A（月火金・早出）", "full_time", "weekday_full", "A", "08:30", "17:30", 1.0, 8.0, 1, "開院準備を担当"),
("weekday_full_B", "正職員B（月火金・通常）", "full_time", "weekday_full", "B", "08:45", "18:45", 2.0, 8.0, 1, "標準シフト、基本2時間連続休憩"),
("weekday_full_C", "正職員C（月火金・遅出）", "full_time", "weekday_full", "C", "09:15", "19:15", 2.0, 8.0, 1, "閉院作業・締め、基本2時間連続休憩"),

# 水曜日の3パターン
("wednesday_early", "正職員◯（水曜・早出）", "full_time", "wednesday", "◯", "08:30", "16:30", 1.0, 7.0, 1, "水曜午後は16:30まで"),
("wednesday_normal", "正職員通常（水曜）", "full_time", "wednesday", "通常", "08:45", "17:45", 2.0, 7.0, 1, "標準シフト、基本2時間連続休憩"),
("wednesday_late", "正職員△（水曜・遅出）", "full_time", "wednesday", "△", "09:15", "18:15", 2.0, 7.0, 1, "午後診療終了後の残務対応、基本2時間連続休憩"),

# 木土の3パターン
("thu_sat_1", "正職員1（木土・早出）", "full_time", "thu_sat", "1", "08:30", "12:30", 0.0, 4.0, 0, "開院準備を担当"),
("thu_sat_2", "正職員2（木土・通常）", "full_time", "thu_sat", "2", "08:45", "12:45", 0.0, 4.0, 0, "標準シフト"),
("thu_sat_3", "正職員3（木土・遅出）", "full_time", "thu_sat", "3", "09:00", "13:00", 0.0, 4.0, 0, "午前診療後の片付け"),
```

2. `EmploymentPattern`にフィールド追加:

```python
@dataclass(slots=True)
class EmploymentPattern:
    # ...既存フィールド...
    day_type: Optional[str] = None          # 'weekday_full', 'wednesday', 'thu_sat', 'all_days'
    pattern_symbol: Optional[str] = None    # 'A', 'B', 'C', '◯', '△', '1', '2', '3'
```

**工数**: 1日

#### 1-2. 最適化エンジンの改修（最重要）

**ファイル**: `optimizer.py`

**作業内容**:

1. **勤務パターン選択関数の実装**:

```python
from typing import Dict, List, Optional
from collections import defaultdict

def _get_available_patterns_for_day(date: str, employee: Employee) -> List[str]:
    """指定日の曜日に応じて利用可能な勤務パターンIDのリストを返す
    
    Args:
        date: YYYY-MM-DD形式の日付文字列
        employee: 職員オブジェクト
    
    Returns:
        利用可能な勤務パターンIDのリスト
        
    Note:
        - 正職員でis_pattern_fixed=Falseの場合のみ複数パターンを返す
        - それ以外は登録済みのemployment_pattern_idを返す
    """
    # パターン固定の場合は登録済みパターンを返す
    if employee.is_pattern_fixed or not employee.employment_pattern_id:
        return [employee.employment_pattern_id] if employee.employment_pattern_id else []
    
    # 曜日判定
    date_obj = datetime.strptime(date, "%Y-%m-%d")
    weekday = date_obj.weekday()  # 0=月, 1=火, 2=水, 3=木, 4=金, 5=土, 6=日
    
    # 曜日別にパターンを返す
    if weekday in [0, 1, 4]:  # 月火金
        return ["weekday_full_A", "weekday_full_B", "weekday_full_C"]
    elif weekday == 2:  # 水曜
        return ["wednesday_early", "wednesday_normal", "wednesday_late"]
    elif weekday in [3, 5]:  # 木土
        return ["thu_sat_1", "thu_sat_2", "thu_sat_3"]
    else:
        return []


def _select_best_pattern(
    employee: Employee,
    date: str,
    available_patterns: List[str],
    pattern_usage_count: Dict[tuple[int, str], int],
) -> str:
    """勤務パターンの偏りを防止しつつ最適なパターンを選択
    
    Args:
        employee: 職員オブジェクト
        date: 日付文字列
        available_patterns: 利用可能なパターンIDリスト
        pattern_usage_count: (employee_id, pattern_id) -> 使用回数のマッピング
    
    Returns:
        選択された勤務パターンID
    """
    if not available_patterns:
        return None
    
    # 各パターンの使用回数を確認
    usage_counts = []
    for pattern_id in available_patterns:
        count = pattern_usage_count.get((employee.id, pattern_id), 0)
        usage_counts.append((pattern_id, count))
    
    # 最小使用回数のパターンから選択（偏り防止）
    min_count = min(count for _, count in usage_counts)
    least_used = [pid for pid, count in usage_counts if count == min_count]
    
    # 同率の場合は先頭を返す（シンプルな実装）
    return least_used[0]


# GeneratedShift生成時にemployment_pattern_idを追加
@dataclass(slots=True)
class GeneratedShift:
    # ... 既存フィールド ...
    employment_pattern_id: Optional[str] = None  # 追加
```

2. **`_assign_employees_to_slot`関数の改修**:

```python
def _assign_employees_to_slot(
    available: List[Employee],
    slot: TimeSlot,
    date_str: str,
    optimisation_mode: str,
    work_count: Dict[int, int],
    morning_workers: List[int],
    pattern_usage_count: Dict[tuple[int, str], int],  # 追加
) -> List[tuple[Employee, str]]:  # 戻り値を(Employee, pattern_id)のタプルに変更
    """職員と勤務パターンのペアを割り当てる"""
    required = slot.required_staff
    selected_employees: List[Employee] = []
    
    # 午後の場合、午前勤務者を優先
    if slot.period == "afternoon" and morning_workers:
        afternoon_capable = [e for e in available if e.id in morning_workers]
        if afternoon_capable:
            needed = min(len(afternoon_capable), required)
            selected_employees = _select_employees_for_slot(
                afternoon_capable, slot, needed, work_count, optimisation_mode
            )
    
    # 残りの枠を埋める
    if len(selected_employees) < required:
        remaining_available = [e for e in available if e not in selected_employees]
        additional_needed = required - len(selected_employees)
        additional = _select_employees_for_slot(
            remaining_available, slot, additional_needed, work_count, optimisation_mode
        )
        selected_employees.extend(additional)
    
    # 各職員に勤務パターンを割り当て
    assignments: List[tuple[Employee, str]] = []
    for employee in selected_employees:
        available_patterns = _get_available_patterns_for_day(date_str, employee)
        pattern_id = _select_best_pattern(
            employee, date_str, available_patterns, pattern_usage_count
        )
        assignments.append((employee, pattern_id))
        
        # パターン使用回数を更新
        if pattern_id:
            key = (employee.id, pattern_id)
            pattern_usage_count[key] = pattern_usage_count.get(key, 0) + 1
    
    return assignments
```

3. **`generate_shifts`関数の改修**:

```python
def generate_shifts(
    employees: Sequence[Employee],
    time_slots: Sequence[TimeSlot],
    start_date: str,
    end_date: str,
    *,
    optimisation_mode: str = "balance",
) -> List[GeneratedShift]:
    _validate_shift_inputs(employees, time_slots, start_date, end_date)
    
    start = datetime.strptime(start_date, "%Y-%m-%d")
    end = datetime.strptime(end_date, "%Y-%m-%d")
    
    schedule: List[GeneratedShift] = []
    work_count: Dict[int, int] = {emp.id: 0 for emp in employees}
    pattern_usage_count: Dict[tuple[int, str], int] = {}  # 追加: (employee_id, pattern_id) -> count
    
    # ... 既存のロジック ...
    
    # _assign_employees_to_slotの呼び出しを変更
    assignments = _assign_employees_to_slot(
        available, slot, date_str, optimisation_mode, work_count, morning_workers,
        pattern_usage_count  # 追加
    )
    
    # GeneratedShift生成時にemployment_pattern_idを設定
    for employee, pattern_id in assignments:
        shift = GeneratedShift(
            date=date_str,
            time_slot_id=slot.id,
            employee_id=employee.id,
            employee_name=employee.name,
            time_slot_name=slot.display_name,
            start_time=slot.start_time,
            end_time=slot.end_time,
            skill_score=calculate_skill_score(employee, slot),
            employee=employee,
            time_slot=slot,
            employment_pattern_id=pattern_id,  # 追加
        )
        schedule.append(shift)
        work_count[employee.id] += 1
    
    return schedule
```

**工数**: 3-5日

#### 1-3. データモデルの更新

**ファイル**: `models.py`

**作業内容**:

```python
@dataclass(slots=True)
class EmploymentPattern:
    # ... 既存フィールド ...
    day_type: Optional[str] = None          # 追加
    pattern_symbol: Optional[str] = None    # 追加

@dataclass(slots=True)
class GeneratedShift:
    # ... 既存フィールド ...
    employment_pattern_id: Optional[str] = None  # 追加
```

**工数**: 0.5日

---

### Phase 2: 高優先度（High対応）

#### 2-1. シフト表示画面の凡例追加

**ファイル**: `pages/4_📋_シフト表示.py`

**作業内容**:

```python
# 勤務パターンの凡例マッピング（拡張版）
pattern_legend = {
    # 月火金
    "weekday_full_A": "A",
    "weekday_full_B": "B",
    "weekday_full_C": "C",
    
    # 水曜日
    "wednesday_early": "◯",
    "wednesday_normal": "通常",
    "wednesday_late": "△",
    
    # 木土
    "thu_sat_1": "1",
    "thu_sat_2": "2",
    "thu_sat_3": "3",
    
    # 時短・パート
    "short_time": "短",
    "part_morning_early": "パ早",
    "part_morning": "パ",
    "part_morning_ext": "パ延",
}

# 凡例表示部分を拡張
st.markdown("### 📖 凡例")

col1, col2, col3, col4 = st.columns(4)

with col1:
    st.markdown("**月火金:**")
    st.markdown("- **A**: 早出 (08:30-17:30)")
    st.markdown("- **B**: 通常 (08:45-18:45)")
    st.markdown("- **C**: 遅出 (09:15-19:15)")

with col2:
    st.markdown("**水曜日:**")
    st.markdown("- **◯**: 早出 (08:30-16:30)")
    st.markdown("- **通常**: 通常 (08:45-17:45)")
    st.markdown("- **△**: 遅出 (09:15-18:15)")

with col3:
    st.markdown("**木土:**")
    st.markdown("- **1**: 早出 (08:30-12:30)")
    st.markdown("- **2**: 通常 (08:45-12:45)")
    st.markdown("- **3**: 遅出 (09:00-13:00)")

with col4:
    st.markdown("**その他:**")
    st.markdown("- **短**: 時短勤務")
    st.markdown("- **パ**: パート")
    st.markdown("- **-**: 休み")
```

**工数**: 0.5日

#### 2-2. シフト生成画面の説明追加

**ファイル**: `pages/3_🎯_シフト生成.py`

**作業内容**:

最適化モードの説明を拡張:

```python
optimization_mode = st.selectbox(
    "最適化モード",
    options=["balance", "skill", "days"],
    format_func=lambda x: {
        "balance": "⚖️ バランス（勤務回数とスキルの両方を考慮）",
        "skill": "🎯 スキル重視（目標スキルスコアに近づける）",
        "days": "📅 日数重視（勤務回数の均等化を優先）"
    }[x],
    index=0,
    help="""
    **バランス**: 勤務回数とスキル能力の両方を考慮して最適化します（推奨）
    **スキル重視**: 各時間帯のスキル能力を平均化し、日によるサービス品質の偏りを防止します
    **日数重視**: 職員の勤務回数をできるだけ均等にすることを優先します
    
    **正職員の勤務パターン**: シフト生成時に早番・中番・遅番を自動的に割り当てます。
    同一職員が特定のパターンに偏らないよう配慮されます。
    """
)
```

**工数**: 0.5日

#### 2-3. 設計書の更新

**ファイル**: 各設計書

**作業内容**:
- 実装済みの内容に合わせて設計書を更新
- 特に`optimizer.py`の動作フローを詳細化

**工数**: 1日

---

### Phase 3: 中優先度（Medium対応）

#### 3-1. ドキュメントの更新

**ファイル**: `docs/ALGORITHM.md`, `docs/USER_GUIDE.md`

**作業内容**:
- 正職員の勤務パターン選択アルゴリズムの詳細を記述
- ユーザーガイドに動作説明を追加

**工数**: 1日

#### 3-2. 統計情報の拡張

**ファイル**: `optimizer.py`, `pages/3_🎯_シフト生成.py`

**作業内容**:

```python
def calculate_pattern_distribution(shifts: Sequence[GeneratedShift]) -> Dict[str, Dict[str, int]]:
    """勤務パターンの分布を計算
    
    Returns:
        {employee_id: {pattern_id: count}}
    """
    distribution = defaultdict(lambda: defaultdict(int))
    for shift in shifts:
        if shift.employment_pattern_id:
            distribution[shift.employee_id][shift.employment_pattern_id] += 1
    return dict(distribution)
```

生成結果に統計情報を追加:
```python
# シフト生成成功後
pattern_dist = calculate_pattern_distribution(result_shifts)

with st.expander("📊 勤務パターン分布"):
    for emp in employees:
        if emp.id in pattern_dist:
            st.markdown(f"**{emp.name}**")
            for pattern_id, count in pattern_dist[emp.id].items():
                st.markdown(f"  - {pattern_id}: {count}回")
```

**工数**: 1日

---

## 📊 修正工数見積もり

| Phase | 作業項目 | 工数 | 優先度 |
|-------|---------|------|--------|
| **Phase 1** | **最重要（必須）** | **5-6.5日** | **Critical** |
| 1-1 | 勤務パターンマスタ追加 | 1日 | Critical |
| 1-2 | 最適化エンジン改修 | 3-5日 | Critical |
| 1-3 | データモデル更新 | 0.5日 | Critical |
| **Phase 2** | **高優先度（推奨）** | **2日** | **High** |
| 2-1 | シフト表示画面の凡例追加 | 0.5日 | High |
| 2-2 | シフト生成画面の説明追加 | 0.5日 | High |
| 2-3 | 設計書の更新 | 1日 | High |
| **Phase 3** | **中優先度（将来）** | **2日** | **Medium** |
| 3-1 | ドキュメント更新 | 1日 | Medium |
| 3-2 | 統計情報拡張 | 1日 | Medium |
| **合計** | | **9-10.5日** | |

---

## ✅ チェックリスト

### Phase 1完了条件

- [ ] `database.py`に月火金・水曜・木土の勤務パターンが追加されている
- [ ] `models.py`に`day_type`, `pattern_symbol`フィールドが追加されている
- [ ] `optimizer.py`に`_get_available_patterns_for_day`関数が実装されている
- [ ] `optimizer.py`に`_select_best_pattern`関数が実装されている
- [ ] `_assign_employees_to_slot`が勤務パターンを返すよう改修されている
- [ ] `generate_shifts`がパターン使用回数を追跡している
- [ ] `GeneratedShift`に`employment_pattern_id`フィールドが追加されている
- [ ] 正職員でシフト生成時に異なるパターンが割り当てられることを確認
- [ ] 同一職員のパターンが偏らないことを確認

### Phase 2完了条件

- [ ] シフト表示画面に月火金・水曜・木土の凡例が表示される
- [ ] シフト生成画面に勤務パターン変動の説明が追加されている
- [ ] 設計書が最新の実装に更新されている

### Phase 3完了条件

- [ ] ドキュメントが更新されている
- [ ] 勤務パターン分布の統計情報が表示される

---

## 🎯 推奨アクション

### 即座に実施すべき対応

1. **Phase 1の全項目を最優先で実施**
   - 現状では要求仕様の核心機能が動作していない
   - Phase 1完了なしにシステムは要求を満たさない

2. **テストケースの追加**
   ```python
   def test_pattern_distribution():
       """正職員の勤務パターンが偏らないことを確認"""
       # ... テストコード ...
   ```

3. **マイグレーション計画**
   - 既存のDBデータに影響がないよう慎重に実施
   - `_seed_employment_patterns`の実行タイミング確認

### 段階的な実施推奨

1. **Phase 1-1**: データ基盤の整備（1日）
2. **Phase 1-2, 1-3**: 最適化ロジックの実装（3-5日）
3. **Phase 2**: UI改善（2日）
4. **Phase 3**: ドキュメント整備（2日）

---

## 📚 参考資料

### 関連ドキュメント

- [REQUIREMENTS.md](../docs/REQUIREMENTS.md) - セクション3.3「勤務形態」
- [ALGORITHM.md](../docs/ALGORITHM.md) - 最適化アルゴリズム詳細
- [シフト生成画面詳細設計書](../design/シフト生成画面_詳細設計書.md)
- [職員管理画面詳細設計書](../design/職員管理画面_詳細設計書.md)

### 実装ファイル

- `src/shift_scheduler/optimizer.py` - 最適化エンジン（要改修）
- `src/shift_scheduler/database.py` - データベース層（パターンマスタ追加）
- `src/shift_scheduler/models.py` - データモデル（フィールド追加）
- `pages/3_🎯_シフト生成.py` - シフト生成画面
- `pages/4_📋_シフト表示.py` - シフト表示画面

---

## 📝 まとめ

### 現状の評価

- ✅ **データモデル層**: 完全対応済み
- ❌ **最適化エンジン**: **未実装**（最重要課題）
- ✅ **休憩スケジューラ**: 対応済み
- ⚠️ **UI層**: 部分的対応（表示改善の余地あり）
- ⚠️ **設計書**: 記述あるが実装と乖離

### 最優先課題

**`optimizer.py`の改修が絶対必要**です。現状では要求仕様の核心機能が動作していません。

Phase 1の実装により、正職員の勤務パターンが日ごとに変動し、同一職員が特定のパターンに偏らない仕組みが実現されます。

### 期待される効果

Phase 1完了後:
- 正職員の負担が公平に分散される
- 開院準備・閉院作業の担当が固定化されない
- スキルバランスと勤務パターンのバランスが最適化される
- 要求仕様に完全準拠したシフト生成が可能になる

---

**レポート作成者**: AI Assistant  
**作成日**: 2026年1月4日  
**バージョン**: 1.0
